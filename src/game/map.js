export const Map = {

    create: (x, y, width, height, tileWidth, tileHeight, data = []) => ({
        x,
        y,
        width,
        height,
        tileWidth,
        tileHeight,
        data
    })
}